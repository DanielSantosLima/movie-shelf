import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    screenContainer: {
      flex: 1,
      backgroundColor: "#fff",
    },
    titleContainer: {
      alignItems: "center",
      marginTop: "5%",
    },
    title: {
      fontWeight: "bold",
      fontSize: 20,
    },
    imageItem: {
      width: 100, // Set a fixed width for testing
      height: 100, // Set a fixed height for testing
      margin: 1,
    },
  });
  
  export default styles;
  